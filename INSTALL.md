# 📦 Полная инструкция по установке AutoPosst

## Требования

- **ОС**: Windows 10/11 (64-bit)
- **Python**: 3.9 или выше
- **Интернет**: для работы с API
- **Дисковое пространство**: ~500 МБ

## Шаг 1: Установка Python

Если Python не установлен:

1. Скачайте Python с [python.org](https://www.python.org/downloads/)
2. При установке **обязательно** отметьте "Add Python to PATH"
3. Проверьте установку:
```bash
python --version
```
Должно показать версию 3.9 или выше

## Шаг 2: Установка зависимостей

Откройте командную строку (PowerShell или CMD) в папке проекта:

```bash
# Обновить pip
python -m pip install --upgrade pip

# Установить зависимости
pip install -r requirements.txt

# Установить браузер для Dzen
playwright install chromium
```

**Важно**: Установка может занять 5-10 минут, особенно sentence-transformers и playwright.

## Шаг 3: Проверка установки

Проверьте что всё установилось:

```bash
python -c "import PySide6; print('PySide6 OK')"
python -c "import google.generativeai; print('Gemini OK')"
python -c "from playwright.sync_api import sync_playwright; print('Playwright OK')"
python -c "from PIL import Image; print('Pillow OK')"
```

Если все команды выводят "OK" - установка успешна!

## Шаг 4: Первый запуск

```bash
python main.py
```

При первом запуске:
- Создастся папка `data/` с базой данных
- Создадутся конфигурационные файлы
- Откроется окно приложения

## Решение проблем установки

### Ошибка "pip не найден"
```bash
python -m pip install -r requirements.txt
```

### Ошибка при установке playwright
```bash
pip install playwright
playwright install chromium
```

### Ошибка "Microsoft Visual C++ required"
Скачайте и установите:
- [Visual C++ Redistributable](https://aka.ms/vs/17/release/vc_redist.x64.exe)

### Ошибка при установке sentence-transformers
```bash
pip install torch --index-url https://download.pytorch.org/whl/cpu
pip install sentence-transformers
```

### Ошибка "Permission denied"
Запустите командную строку от имени администратора

## Сборка в один EXE файл

После установки можно собрать в один исполняемый файл:

```bash
# Установить PyInstaller если еще не установлен
pip install pyinstaller

# Собрать
pyinstaller --onefile --windowed --name AutoPosst --icon=icon.ico main.py
```

EXE будет в папке `dist/AutoPosst.exe`

**Примечание**: При первом запуске EXE может быть медленным (распаковка), это нормально.

## Обновление

Для обновления:

```bash
# Обновить зависимости
pip install -r requirements.txt --upgrade

# Обновить playwright
playwright install chromium --force
```

## Удаление

Просто удалите папку проекта. Все данные в `data/` можно сохранить для переноса на другую машину.

---

**Готово! Теперь переходите к [QUICK_START.md](QUICK_START.md) для настройки и запуска.**

